# 🚀 Інструкція з деплою сайту БлискКлінінг

## Швидкий старт

Ваш сайт готовий до деплою! Виберіть один із варіантів нижче.

---

## 📦 Варіант 1: Vercel (РЕКОМЕНДОВАНО)

Vercel - найкращий варіант для React додатків з Supabase.

### Крок 1: Завантажте код на GitHub

1. **Створіть новий репозиторій:**
   - Перейдіть на https://github.com/new
   - Назва: `blisk-cleaning` (або будь-яка інша)
   - Тип: `Public` або `Private`
   - **НЕ** додавайте README, .gitignore чи ліцензію
   - Натисніть "Create repository"

2. **Завантажте файли:**
   - Завантажте весь проект як ZIP з Figma Make
   - Розпакуйте на вашому комп'ютері
   - Відкрийте термінал/командний рядок у папці проекту
   
3. **Виконайте команди:**
   ```bash
   git init
   git add .
   git commit -m "🎉 Initial commit - БлискКлінінг сайт"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/blisk-cleaning.git
   git push -u origin main
   ```
   
   > 💡 Замініть `ВАШ_USERNAME` на ваш логін GitHub

### Крок 2: Деплой на Vercel

1. **Перейдіть на Vercel:**
   - https://vercel.com/signup
   - Увійдіть через GitHub

2. **Імпортуйте проект:**
   - Натисніть "Add New" → "Project"
   - Виберіть репозиторій `blisk-cleaning`
   - Натисніть "Import"

3. **Налаштуйте проект:**
   - **Framework Preset:** Vite (має визначитись автоматично)
   - **Root Directory:** `./` (залишіть як є)
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`

4. **Додайте змінні середовища:**
   
   Розгорніть розділ "Environment Variables" і додайте:
   
   ```
   VITE_SUPABASE_URL=ваш_supabase_url
   VITE_SUPABASE_ANON_KEY=ваш_anon_key
   ```
   
   > 🔑 Знайти ці значення можна в Supabase Dashboard:
   > - Перейдіть на https://app.supabase.com
   > - Виберіть ваш проект
   > - Settings → API
   > - Скопіюйте "Project URL" та "anon public" ключ

5. **Запустіть деплой:**
   - Натисніть "Deploy"
   - Зачекайте 2-3 хвилини
   - Готово! Отримаєте URL: `https://your-project.vercel.app`

### Крок 3: Деплой Supabase Edge Functions

1. **Встановіть Supabase CLI:**
   
   Windows (PowerShell):
   ```powershell
   scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
   scoop install supabase
   ```
   
   macOS/Linux:
   ```bash
   brew install supabase/tap/supabase
   ```
   
   Або через NPM:
   ```bash
   npm install -g supabase
   ```

2. **Увійдіть в Supabase:**
   ```bash
   supabase login
   ```

3. **Підключіться до проекту:**
   ```bash
   supabase link --project-ref ваш_project_id
   ```
   
   > Project ID можна знайти в Settings → General

4. **Задеплойте функції:**
   ```bash
   supabase functions deploy server
   ```

5. **Перевірте:**
   - Функція буде доступна за адресою:
   - `https://ваш_project_id.supabase.co/functions/v1/make-server-4e0b1fee/`

---

## 📦 Варіант 2: Netlify

Альтернатива Vercel з схожим функціоналом.

1. **Завантажте на GitHub** (див. Крок 1 з Vercel)

2. **Перейдіть на Netlify:**
   - https://app.netlify.com/signup
   - Увійдіть через GitHub

3. **Додайте новий сайт:**
   - "Add new site" → "Import an existing project"
   - Виберіть GitHub
   - Виберіть репозиторій `blisk-cleaning`

4. **Налаштування:**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Додайте змінні середовища (як у Vercel)

5. **Deploy site** → Готово!

---

## 📦 Варіант 3: GitHub Pages (Простий, але обмежений)

⚠️ GitHub Pages **НЕ** підтримує серверні функції, тому деякі функції можуть не працювати.

1. **Завантажте на GitHub** (див. вище)

2. **Увімкніть GitHub Pages:**
   - У репозиторії → Settings → Pages
   - Source: "GitHub Actions"

3. **Створіть файл `.github/workflows/deploy.yml`:**
   ```yaml
   name: Deploy to GitHub Pages
   
   on:
     push:
       branches: [ main ]
   
   jobs:
     build-and-deploy:
       runs-on: ubuntu-latest
       
       steps:
       - uses: actions/checkout@v3
       
       - name: Setup Node
         uses: actions/setup-node@v3
         with:
           node-version: '18'
           
       - name: Install dependencies
         run: npm install
         
       - name: Build
         run: npm run build
         env:
           VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
           VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
         
       - name: Deploy
         uses: peaceiris/actions-gh-pages@v3
         with:
           github_token: ${{ secrets.GITHUB_TOKEN }}
           publish_dir: ./dist
   ```

4. **Додайте секрети:**
   - Settings → Secrets and variables → Actions
   - Додайте `VITE_SUPABASE_URL` та `VITE_SUPABASE_ANON_KEY`

5. **Push змін** → сайт автоматично задеплоїться!

---

## ✅ Після деплою - Checklist

- [ ] Сайт відкривається за URL
- [ ] Всі сторінки працюють (Головна, Ціни, Галерея, Блог, Відгуки)
- [ ] Адмін панель доступна за `/admin`
- [ ] Форми працюють (Замовлення, Контакти, Відгуки)
- [ ] Зображення завантажуються
- [ ] Сайт адаптивний на мобільних
- [ ] Перевірте консоль браузера на помилки (F12)

---

## 🔧 Автоматичні оновлення

Після підключення до Vercel/Netlify:

1. Внесіть зміни в код
2. Збережіть та закомітьте:
   ```bash
   git add .
   git commit -m "Оновлення контенту"
   git push
   ```
3. Сайт автоматично оновиться за 1-2 хвилини!

---

## 🆘 Проблеми?

### Помилка: "Environment variables not found"
- Переконайтесь, що додали `VITE_SUPABASE_URL` та `VITE_SUPABASE_ANON_KEY`
- Префікс `VITE_` обов'язковий для Vite!

### Помилка: "Failed to fetch"
- Перевірте, що Edge функції задеплоєні в Supabase
- Перевірте налаштування CORS в Supabase

### Білий екран після деплою
- Відкрийте консоль браузера (F12)
- Перевірте помилки
- Найчастіше - проблема зі змінними середовища

### Адмін панель не працює
- Перевірте, що в KV store є запис `admin_password`
- Стандартний пароль: `admin123` (змініть після входу!)

---

## 📞 Додаткові ресурси

- [Vercel Documentation](https://vercel.com/docs)
- [Netlify Documentation](https://docs.netlify.com)
- [Supabase CLI Docs](https://supabase.com/docs/guides/cli)
- [GitHub Pages Guide](https://docs.github.com/en/pages)

---

## 🎉 Готово!

Ваш сайт БлискКлінінг тепер доступний в інтернеті! 

**Не забудьте:**
- Змінити пароль адміна
- Налаштувати контент через адмін панель
- Додати реальні фото послуг
- Налаштувати контактну інформацію

Успіхів! 🧹✨
