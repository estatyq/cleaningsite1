# Усунення несправностей 🔧

## Помилки авторизації в адмінці

### Проблема: "Error: Unauthorized" або "Failed to fetch"

**Можливі причини**:
1. Неправильний пароль
2. Пароль не передається до API
3. Edge Function не запущена
4. Проблеми з мережею

**Рішення**:

#### 1. Перевірте пароль
Стандартний пароль за замовчуванням: `admin123`

**Як перевірити**:
1. Відкрийте консоль браузера (F12)
2. Введіть пароль і спробуйте увійти
3. Перегляньте логи - якщо пароль неправильний, ви побачите помилку

Ви можете змінити його в файлі `/supabase/functions/server/index.tsx` на рядку 24:
```typescript
const ADMIN_PASSWORD = "admin123"; // Змініть на свій пароль
```

#### 2. Перевірте Edge Function

**Симптоми**: "Failed to fetch", "Network error"

**Перевірка**:
1. Відкрийте Supabase Dashboard
2. Перейдіть в Edge Functions → server
3. Перевірте чи функція запущена (статус: Active)
4. Перегляньте логи на наявність помилок

**Якщо функція не запущена**:
- Edge Function автоматично розгортається при першому запиті
- Зачекайте 10-15 секунд і спробуйте ще раз
- Перевірте що всі файли у `/supabase/functions/server/` на місці

#### 3. Перевірте передачу пароля

Пароль зберігається в стані AdminPanel після входу і передається всім компонентам через props:

```typescript
// AdminPanel.tsx
const [password, setPassword] = useState('');

// Передача в компоненти:
<ServicesManager password={password} />
<ContactsManager password={password} />
<OrdersManager password={password} />
```

**Відлагодження**:
1. Відкрийте консоль браузера (F12 → Console)
2. Шукайте логи типу "Loading orders with password: ***"
3. Якщо бачите "EMPTY" - вийдіть та увійдіть знову

---

## Дані не синхронізуються

### Проблема: Зміни в адмінці не відображаються на сайті

**Рішення**:
1. **Оновіть сторінку** (F5 або Ctrl+R)
2. **Очистіть кеш браузера** (Ctrl+Shift+Delete)
3. **Перевірте консоль** на наявність помилок API

### Проблема: Телефон в Header не змінюється

Переконайтеся що:
1. Ви зберегли зміни в ContactsManager
2. Оновили головну сторінку
3. Перший номер у масиві phones не пустий

Код Header.tsx автоматично бере перший номер:
```typescript
if (contactsRes.data?.phones?.length > 0) {
  setPhoneNumber(contactsRes.data.phones[0].number);
}
```

---

## Помилки при швидкому заповненні

### Проблема: "Unauthorized" при натисканні "Швидке заповнення"

**Причина**: Пароль не передається правильно до функцій ініціалізації.

**Рішення**:
1. Вийдіть з адмінки
2. Увійдіть знову з правильним паролем
3. Спробуйте ще раз

**Технічні деталі**:
AdminWelcome компонент отримує пароль через props:
```typescript
<AdminWelcome onTabChange={setActiveTab} password={password} />
```

Якщо password undefined або порожній, швидке заповнення не спрацює.

---

## Замовлення не зберігаються

### Проблема: Помилка при створенні замовлення

**Перевірте**:
1. Заповнені всі обов'язкові поля (ім'я, телефон, послуга)
2. У адмінці є хоча б одна послуга (інакче форма не працює)
3. Консоль браузера для деталей помилки

**API endpoint**: `POST /orders` (не потребує авторизації)

Замовлення зберігаються в KV store під ключем "orders" як масив.

---

## База даних

### Структура KV Store

Всі дані зберігаються в Supabase KV store:

**Ключі**:
- `service:{id}` - окремі послуги
- `contacts` - контактна інформація
- `branding` - логотип та назва компанії
- `social_media` - посилання на соцмережі
- `hero_images` - зображення для Hero секції
- `discount_settings` - налаштування знижки
- `price:{category}` - ціни по категоріях
- `review:{id}` - окремі відгуки
- `gallery:{id}` - елементи галереї
- `blog:{id}` - статті блогу
- `orders` - масив замовлень

### Якщо база даних порожня

1. Зайдіть в адмінку
2. Натисніть "Швидке заповнення" на головній сторінці
3. Почекайте завершення (2-5 секунд)
4. Перевірте вкладки - дані мають з'явитися

---

## Логування для відлагодження

### Увімкнені логи на сервері

Сервер логує всі запити через hono/logger:
```typescript
app.use('*', logger(console.log));
```

### Перевірити логи сервера

Логи Edge Function можна подивитися в Supabase Dashboard:
1. Відкрийте Supabase проект
2. Перейдіть в Edge Functions → server
3. Відкрийте вкладку Logs

### Перевірити логи клієнта

Відкрийте консоль браузера (F12) і перевірте:
- Червоні помилки (errors)
- Помаранчеві попередження (warnings)
- Сині інформаційні повідомлення (logs)

---

## Часті питання

### Як змінити пароль адміна?

1. Відкрийте `/supabase/functions/server/index.tsx`
2. Знайдіть рядок 24: `const ADMIN_PASSWORD = "admin123";`
3. Змініть на свій пароль
4. Збережіть файл
5. Edge Function автоматично перезавантажиться

### Як додати нове поле в контакти?

1. Оновіть інтерфейс `Contacts` в `/components/admin/ContactsManager.tsx`
2. Додайте поле в форму
3. Оновіть API endpoint `/contacts` в сервері
4. Оновіть компоненти що використовують контакти (Header, Footer, ContactForm)

### Як видалити всі дані?

⚠️ **УВАГА**: Цю операцію не можна скасувати!

Немає вбудованої функції для повного очищення. Ви можете:
1. Вручну видалити кожен елемент через адмінку
2. Або написати скрипт що викличе `kv.del()` для кожного ключа

---

## Підтримка

Якщо проблема не вирішена:
1. Перевірте консоль браузера (F12)
2. Перевірте логи Edge Function в Supabase
3. Перевірте чи правильно налаштований Supabase проект
4. Перевірте змінні середовища (SUPABASE_URL, SUPABASE_ANON_KEY)
