# 🚨 ЕКСТРЕНЕ СКИДАННЯ ПАРОЛЯ

## Проблема
Пароль в базі даних встановлений як "qwerty", але очікується "admin123"

## ✅ ШВИДКЕ РІШЕННЯ (2 способи)

### Спосіб 1: Через браузер (найпростіший)

1. Відкрийте консоль браузера (F12 → Console)
2. Вставте цей код:

```javascript
// Скидання пароля до admin123
fetch('https://YOUR-PROJECT-ID.supabase.co/functions/v1/make-server-4e0b1fee/reset-password', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  }
})
.then(res => res.json())
.then(data => {
  console.log('✅ Результат:', data);
  if (data.success) {
    alert('✅ Пароль скинуто до "admin123"!');
  }
})
.catch(err => console.error('❌ Помилка:', err));
```

3. **ВАЖЛИВО**: Замініть `YOUR-PROJECT-ID` на ваш реальний Project ID
4. Натисніть Enter
5. Дочекайтесь повідомлення "Пароль скинуто до admin123"

### Спосіб 2: Через адмін панель

1. Зайдіть на сторінку `/admin` вашого сайту
2. **НЕ потрібно вводити пароль** - просто натисніть на посилання внизу форми авторизації
3. Натисніть кнопку "Скинути пароль до стандартного"
4. Підтвердіть дію
5. Зачекайте повідомлення про успішне скидання
6. Тепер можете увійти з паролем `admin123`

## Після скидання

1. Увійдіть в адмін панель з паролем: `admin123`
2. Перейдіть на вкладку "Акаунт"
3. Змініть пароль на свій власний
4. **НЕ використовуйте прості паролі** як "qwerty", "123456" тощо

## Чому це сталося?

Хтось раніше змінив пароль через адмін панель на "qwerty". Це небезпечний пароль!

## Безпека

⚠️ **ВАЖЛИВО**: Після скидання обов'язково змініть пароль на складний:
- Мінімум 8 символів
- Використовуйте великі та малі літери
- Додайте цифри та спеціальні символи
- Приклад: `MyStr0ng!Pass2024`

## Технічні деталі

Endpoint `/reset-password` скидає пароль до стандартного значення "admin123" БЕЗ перевірки поточного пароля. Це екстрений механізм відновлення доступу.

---

**Підтримка**: Якщо виникли проблеми, перевірте:
- Edge Function запущена в Supabase Dashboard
- Project ID правильний
- Інтернет з'єднання стабільне
