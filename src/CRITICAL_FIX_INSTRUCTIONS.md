# 🚨 КРИТИЧНЕ ВИПРАВЛЕННЯ - ПОМИЛКА 401

## Проблема

Помилка 401 виникає через використання старого пароля з `sessionStorage` замість пароля з `props`.

## Файл для виправлення

`/components/admin/OrdersManager.tsx`, рядок 112

## Що змінити

**ПЕРЕД (рядок 112):**
```typescript
const effectivePassword = password || sessionStorage.getItem('adminPassword') || '';
```

**ПІСЛЯ:**
```typescript
// Використовуємо ТІЛЬКИ пароль з props, ніяких fallback!
```

І далі на рядку 114 замініть:
```typescript
const response = await getOrders(effectivePassword);
```

На:
```typescript
const response = await getOrders(password);
```

## Покрокова інструкція

1. Відкрийте файл `/components/admin/OrdersManager.tsx`
2. Знайдіть рядок 112 (пошук по тексту: `effectivePassword`)
3. **ВИДАЛІТЬ** рядок 112 повністю
4. **ВИДАЛІТЬ** рядок 113 (логування effectivePassword)
5. На рядку 114 замініть `effectivePassword` на `password`

## Результат

Після виправлення рядки 111-114 мають виглядати так:

```typescript
      console.log('✅ Connection OK, fetching orders...');
      console.log('🔐 Using password from props:', password ? `\"${password}\"` : 'EMPTY');
      const response = await getOrders(password);
      console.log('📦 Orders response:', response);
```

## Після виправлення

1. Збережіть файл
2. Відкрийте адмін панель
3. **ОБОВ'ЯЗКОВО**: Виконайте в консолі браузера:
   ```javascript
   sessionStorage.clear();
   location.reload();
   ```
4. Увійдіть з паролем `admin123`
5. Помилка 401 має зникнути

---

**Дата створення:** 2025-11-10
**Пріоритет:** КРИТИЧНИЙ 🔴
