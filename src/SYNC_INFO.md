# 🔄 Інформація про синхронізацію даних

## Як працює автоматична синхронізація?

Всі дані завантажуються з **Supabase KV Store** через API ендпоінти.

---

## 📊 Послуги (Services)

### Де зберігаються:
- **KV Store:** ключі `service:*`
- **API:** `GET /services`, `POST /services`, `DELETE /services/:id`

### Де відображаються:
1. **Головна сторінка** (`/components/Services.tsx`)
   - Секція "Наші послуги"
   - Всі послуги з БД
   - З описом та параметрами

2. **Футер** (`/components/Footer.tsx`)
   - Секція "Послуги"
   - Перші 4 послуги
   - Посилання на #services

3. **Контактна форма** (`/components/ContactForm.tsx`)
   - Dropdown "Виберіть послугу"
   - Всі послуги з БД
   - Для вибору при замовленні

### Як оновити:
1. Адмін панель → вкладка "Послуги"
2. Додати/Редагувати/Видалити послугу
3. **Автоматично оновлюється** на всіх сторінках!

---

## 📞 Контакти (Contacts)

### Де зберігаються:
- **KV Store:** ключ `contacts`
- **API:** `GET /contacts`, `POST /contacts`

### Структура даних:
```json
{
  "phones": [
    {
      "number": "+380 (12) 345-67-89",
      "viber": true,
      "telegram": true,
      "whatsapp": false
    }
  ],
  "email": "info@example.com",
  "address": "Київ, Україна",
  "schedule": "Пн-Нд: 24/7"
}
```

### Де відображаються:
1. **Футер** (`/components/Footer.tsx`)
   - Секція "Контакти"
   - Всі телефони з іконками месенджерів
   - Email з іконкою
   - Іконки соцмереж

2. **Контактна форма** (`/components/ContactForm.tsx`)
   - Ліва колонка з контактами
   - Телефони з іконками месенджерів
   - Email, адреса, графік роботи

### Іконки месенджерів:
- ✅ **Viber** → фіолетова іконка
- ✅ **Telegram** → синя іконка
- ✅ **WhatsApp** → зелена іконка

Іконки показуються **тільки якщо** галочка стоїть в адмін панелі!

### Як оновити:
1. Адмін панель → вкладка "Контакти"
2. Додати телефони, поставити галочки месенджерів
3. Вказати email, адресу, графік
4. Натиснути "Зберегти контакти"
5. **Автоматично оновлюється** на всіх сторінках!

---

## 💰 Ціни (Pricing)

### Де зберігаються:
- **KV Store:** ключ `pricing_tables`
- **API:** `GET /pricing`, `POST /pricing`, `DELETE /pricing/:tableId`

### Де відображаються:
- **Сторінка цін** (`/pages/PricingPage.tsx`)
- 4 таблиці з різними тарифами

### Як оновити:
1. Адмін панель → вкладка "Ціни"
2. Завантажити приклади або додати власні
3. **Автоматично оновлюється** на сторінці цін!

---

## 📸 Галерея (Gallery)

### Де зберігаються:
- **KV Store:** ключі `gallery:*`
- **API:** `GET /gallery`, `POST /gallery`, `DELETE /gallery/:id`

### Підтримка:
- ✅ Фото (URL посилання)
- ✅ Відео (MP4 або YouTube embed)

### Де відображаються:
- **Сторінка галереї** (`/pages/GalleryPage.tsx`)
- Сітка з фото та відео

### Як оновити:
1. Адмін панель → вкладка "Галерея"
2. Додати URL фото або YouTube embed
3. **Автоматично оновлюється** на сторінці галереї!

---

## 📝 Блог (Blog)

### Де зберігаються:
- **KV Store:** ключі `blog:*`
- **API:** `GET /blog`, `POST /blog`, `DELETE /blog/:id`

### Де відображаються:
- **Сторінка блогу** (`/pages/BlogPage.tsx`)
- Картки постів з фото/відео

### Як оновити:
1. Адмін панель → вкладка "Блог"
2. Створити пост
3. **Автоматично оновлюється** на сторінці блогу!

---

## 💬 Відгуки (Reviews)

### Де зберігаються:
- **KV Store:** ключі `review:*`
- **API:** `GET /reviews`, `POST /reviews`, `PATCH /reviews/:id`, `DELETE /reviews/:id`

### Де відображаються:
1. **Головна сторінка** (`/components/Reviews.tsx`)
   - Секція відгуків
   - Тільки затверджені (approved: true)

2. **Сторінка відгуків** (`/pages/ReviewsPage.tsx`)
   - Всі затверджені відгуки
   - Форма для додавання

### Як оновити:
1. Користувачі залишають відгуки на сайті
2. Адмін панель → вкладка "Відгуки"
3. Затвердити/Відхилити/Видалити
4. **Автоматично оновлюється** на всіх сторінках!

---

## 🎨 Брендинг (Branding)

### Де зберігаються:
- **KV Store:** ключ `branding`
- **API:** `GET /branding`, `POST /branding`

### Структура даних:
```json
{
  "logo": "https://example.com/logo.png",
  "companyName": "БлискКлінінг"
}
```

### Де відображаються:
1. **Хедер** (`/components/Header.tsx`)
   - Логотип + назва компанії
   - Клікабельне - повертає на головну

2. **Футер** (`/components/Footer.tsx`)
   - Логотип + назва компанії
   - Перша секція футера

### Як оновити:
1. Адмін панель → вкладка "Брендинг"
2. Вказати URL логотипа (опціонально)
3. Вказати назву компанії
4. Натиснути "Зберегти брендинг"
5. **Автоматично оновлюється** на всіх сторінках!

---

## 📱 Соціальні мережі (Social Media)

### Де зберігаються:
- **KV Store:** ключ `social_media`
- **API:** `GET /social`, `POST /social`

### Структура даних:
```json
{
  "facebook": {
    "url": "https://facebook.com/page",
    "enabled": true
  },
  "instagram": {
    "url": "https://instagram.com/page",
    "enabled": true
  }
}
```

### Де відображаються:
1. **Футер** (`/components/Footer.tsx`)
   - Секція "Контакти"
   - Іконки Facebook та Instagram
   - Відкриваються в новій вкладці

### Іконки показуються тільки якщо:
- ✅ Галочка "Показувати у футері" стоїть
- URL може бути порожнім (тоді іконка не клікабельна)

### Як оновити:
1. Адмін панель → вкладка "Брендинг"
2. Вказати URL соцмереж
3. Поставити галочки для показу
4. Натиснути "Зберегти соціальні мережі"
5. **Автоматично оновлюється** у футері!

---

## 🖼️ Головні зображення (Hero Images)

### Де зберігаються:
- **KV Store:** ключ `hero_images`
- **API:** `GET /hero-images`, `POST /hero-images`

### Структура даних:
```json
{
  "mainImage": "https://images.unsplash.com/photo-...",
  "secondaryImage": "https://images.unsplash.com/photo-..."
}
```

### Де відображаються:
1. **Головна сторінка** (`/components/Hero.tsx`)
   - Основне зображення - центральне фото
   - Додаткове зображення - можна використати для декору

### Як оновити:
1. Адмін панель → вкладка "Головна"
2. Вказати URL основного зображення
3. Вказати URL додаткового зображення
4. Перегляд попереднього вигляду
5. Натиснути "Зберегти зображення"
6. **Автоматично оновлюється** на головній!

**Корисні ресурси:**
- Unsplash (безкоштовні фото високої якості)
- Pexels (безкоштовні фото)
- Imgur (завантаження власних фото)

---

## 💰 Знижка (Discount)

### Де зберігається:
- **KV Store:** ключ `discount_settings`
- **API:** `GET /discount`, `POST /discount`

### Структура даних:
```json
{
  "enabled": true,
  "percentage": 20,
  "description": "Знижка на перше замовлення"
}
```

### Де відображається:
1. **Головна сторінка** (`/components/Hero.tsx`)
   - Бейдж зі знижкою поруч з основним бейджем
   - Показується тільки якщо `enabled: true`

### Як оновити:
1. Адмін панель → вкладка "Знижка"
2. Ввімкнути/вимкнути показ
3. Вказати відсоток (0-100%)
4. Вказати опис
5. Натиснути "Зберегти знижку"
6. **Автоматично оновлюється** на головній!

**Приклади опису:**
- "Знижка на перше замовлення"
- "Святкова акція"
- "Новорічна знижка"
- "Спеціальна пропозиція"

---

## 🛒 Замовлення (Orders)

### Де зберігається:
- **KV Store:** ключ `orders` (масив об'єктів)
- **API:** 
  - `POST /orders` (публічний, без пароля)
  - `GET /orders` (адмін, з паролем)
  - `PATCH /orders/:id` (адмін, зміна статусу)
  - `DELETE /orders/:id` (адмін, видалення)

### Структура даних:
```json
{
  "id": "order_1699876543210_abc123",
  "name": "Іван Петренко",
  "phone": "+380 98 123 45 67",
  "email": "ivan@example.com",
  "service": "Прибирання квартир",
  "message": "Потрібно прибрати 2-кімнатну квартиру",
  "status": "new",
  "createdAt": "2025-11-10T12:30:00.000Z",
  "updatedAt": "2025-11-10T12:35:00.000Z"
}
```

### Статуси:
- **new** - Нове замовлення (автоматично при створенні)
- **in-progress** - В роботі (встановлюється вручну)
- **completed** - Виконано (встановлюється вручну)

### Як працює:
1. **Клієнт заповнює форму** на головній сторінці
2. Натискає "Відправити замовлення"
3. **Замовлення автоматично зберігається** в базу даних
4. Клієнт бачить підтвердження
5. **Адміністратор** заходить в адмін панель → вкладка "Замовлення"
6. Бачить нові замовлення з статусом "🆕 Нове"
7. Може змінити статус на "⏳ В роботі" або "✅ Виконано"
8. Може видалити виконані замовлення

### Функції адмін панелі:
- ✅ **Статистика** - загальна к-сть, нові, в роботі, виконані
- ✅ **Пошук** - за ім'ям, телефоном, email, послугою
- ✅ **Фільтр** - за статусом (всі/нові/в роботі/виконані)
- ✅ **Клікабельні контакти** - телефон та email
- ✅ **Зміна статусу** - dropdown з емодзі
- ✅ **Видалення** - з підтвердженням
- ✅ **Оновлення** - кнопка "Оновити" для перезавантаження

### Важливо:
- Замовлення сортуються від нових до старих
- Email опціональний (може бути порожнім)
- Повідомлення також опціональне
- Телефон автоматично форматується з +38

---

## ⚡ Швидке заповнення

**Кнопка:** "Швидке заповнення" в AdminWelcome

**Що завантажує:**
- ✅ 6 послуг (прибирання квартир, офісів, генеральне, вікна, хімчистка, після ремонту)
- ✅ Контакти (телефон з всіма месенджерами, email, адреса, графік)
- ✅ Брендинг (назва компанії за замовчуванням)
- ✅ Соціальні мережі (приклади посилань)
- ✅ Головні зображення (2 фото з Unsplash)
- ✅ Знижка (20% за замовчуванням, увімкнена)
- ✅ 4 таблиці цін (вартість, вікна, хімчистка, додаткові)
- ✅ 6 фото/відео в галерею (5 фото + 1 YouTube відео)

**НЕ завантажує (заповнюється автоматично):**
- ❌ Замовлення - зберігаються автоматично при відправці форми клієнтами
- ❌ Відгуки - потребують окремого додавання

**Як використати:**
1. Увійти в адмін панель
2. Натиснути "Швидке заповнення"
3. Почекати 15 секунд
4. ✅ Готово! Сайт повністю заповнений
5. Замовлення будуть з'являтись автоматично при відправці форми

---

## 🎯 Особливості UX

### Клікабельні контакти:
- **Email** - автоматично відкриває поштовий клієнт (mailto:)
- **Адреса** - відкриває Google Maps в новій вкладці
- **Телефони** - клікабельні з автоматичним набором
- **Месенджери** - Viber, Telegram, WhatsApp з прямими посиланнями

### Розумне поле телефону:
Автоматична підстановка +38 для українських номерів:
- `098...` → `+380 98...`
- `0...` → `+380...`
- `380...` → `+380...`
- `80...` → `+380...`
- Визначення українських операторів (39, 50, 63, 66, 67, 68, 73, 91-99)

### Динамічний контент:
- **Рік в копірайті** - автоматично оновлюється (© 2025 → поточний рік)
- **Назва компанії** - синхронізована в копірайті
- Всі зміни без перезавантаження сторінки

---

## 🎯 Підсумок

**Принцип роботи:**
1. Адмін змінює дані в адмін панелі
2. Дані зберігаються в Supabase KV Store
3. Компоненти завантажують дані через API при рендерингу
4. Дані автоматично оновлюються на всіх сторінках

**Без перезавантаження сторінки!**

---

**Останнє оновлення:** 10 листопада 2025  
**Статус:** ✅ Всі компоненти синхронізовані
