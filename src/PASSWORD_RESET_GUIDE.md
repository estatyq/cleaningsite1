# 🔐 Інструкція по скиданню пароля адміністратора

## Проблема
Ви отримуєте помилку **401 Unauthorized** при спробі увійти в адмін панель.

## Швидке рішення

### Варіант 1: Через інтерфейс адмін панелі
1. Відкрийте адмін панель `/admin`
2. При помилці входу з'явиться кнопка **"🔄 Скинути пароль до стандартного"**
3. Натисніть на неї
4. Підтвердіть скидання
5. Використайте стандартний пароль: `admin123`

### Варіант 2: Через API напряму
Відкрийте консоль браузера (F12) та виконайте:

```javascript
// Скинути пароль
fetch('https://wewzibxknwahyykczzzi.supabase.co/functions/v1/make-server-4e0b1fee/reset-password', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Indld3ppYnhrbndhaHl5a2N6enppIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Mzk2MjMyNDAsImV4cCI6MjA1NTE5OTI0MH0.R-QkdNDKBaYCdLQgaTwJBxK0-gfz_5h7lZSzLd0t9EA'
  }
})
.then(res => res.json())
.then(data => console.log('✅ Пароль скинуто:', data))
.catch(err => console.error('❌ Помилка:', err));
```

## Після скидання

Стандартний пароль: **`admin123`**

### Рекомендується:
1. Увійдіть з паролем `admin123`
2. Перейдіть в розділ **"Налаштування облікового запису"**
3. Змініть пароль на свій власний
4. Запишіть новий пароль в безпечному місці

## Якщо нічого не допомогло

Перевірте:
1. Edge Function розгорнута та працює: `/health` endpoint
2. Supabase проект активний
3. Інтернет підключення стабільне
4. Відсутність блокування CORS у браузері

## Технічні деталі

- Функція скидання: `POST /make-server-4e0b1fee/reset-password`
- Не потребує авторизації (для екстрених випадків)
- Скидає пароль до `admin123` в KV store
- Оновлює як базу даних, так і кеш в пам'яті

## Проблеми з pricing даними

Якщо ви бачите помилку:
```
Error loading pricing: TypeError: Cannot read properties of undefined (reading 'replace')
```

**Рішення:**
1. Увійдіть в адмін панель
2. Перейдіть в розділ **"Ціни"**
3. Натисніть кнопку **"Ініціалізувати початкові дані"**
4. Дані будуть завантажені автоматично

## Підтримка

Якщо проблема залишається, перевірте консоль браузера (F12 -> Console) для деталей помилки.
