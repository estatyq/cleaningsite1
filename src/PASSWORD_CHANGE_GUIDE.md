# 🔐 Керівництво зі зміни пароля адміністратора

## Огляд функції

Система тепер підтримує зміну пароля адміністратора безпосередньо через інтерфейс адмін панелі. Це критично важлива функція для забезпечення безпеки вашої CMS системи.

## Основні можливості

### 1. **Зміна пароля через UI**
   - Новий таб "Пароль" в адмін панелі
   - Інтуїтивний інтерфейс з перевіркою безпеки
   - Валідація паролів в реальному часі
   - Показ/приховування паролів за допомогою іконки ока

### 2. **Автоматичне попередження**
   - Банер з попередженням при використанні стандартного пароля
   - Швидкий перехід до зміни пароля одним кліком
   - Індикатор статусу пароля (стандартний/власний)

### 3. **Збереження в KV Store**
   - Пароль зберігається безпечно в Supabase KV
   - Автоматичне завантаження при старті сервера
   - Синхронізація між сесіями

### 4. **Валідація безпеки**
   - Мінімум 6 символів
   - Перевірка поточного пароля
   - Підтвердження нового пароля
   - Новий пароль повинен відрізнятися від поточного

## Як користуватися

### Крок 1: Вхід в адмін панель
```
1. Відкрийте сторінку /admin
2. Введіть поточний пароль (за замовчуванням: admin123)
3. Натисніть "Увійти"
```

### Крок 2: Перехід до зміни пароля
Є три способи:
1. Натисніть на жовтий банер "Змінити зараз" (якщо використовується стандартний пароль)
2. Клікніть на таб "Пароль" в верхній панелі
3. Натисніть на бейдж "🔐 Змініть пароль для безпеки" у Welcome карточці

### Крок 3: Заповнення форми
```
1. Введіть поточний пароль
2. Введіть новий пароль (мінімум 6 символів)
3. Підтвердіть новий пароль
4. Натисніть "Змінити пароль"
```

### Крок 4: Успішна зміна
- Ви отримаєте повідомлення про успіх
- Пароль автоматично оновиться в сесії
- Попередження про стандартний пароль зникне
- Індикатор покаже, що використовується власний пароль

## Технічні деталі

### API Endpoints

#### POST /make-server-4e0b1fee/change-password
Змінює пароль адміністратора.

**Request:**
```json
{
  "currentPassword": "admin123",
  "newPassword": "новий_безпечний_пароль"
}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "Password changed successfully"
}
```

**Response (Error):**
```json
{
  "success": false,
  "error": "Current password is incorrect"
}
```

#### GET /make-server-4e0b1fee/password-status
Отримує статус поточного пароля (потребує авторизації).

**Headers:**
```
X-Admin-Password: ваш_поточний_пароль
```

**Response:**
```json
{
  "success": true,
  "data": {
    "isDefault": false,
    "lastChanged": "2025-11-10T..."
  }
}
```

### Структура зберігання

**KV Store:**
```
Key: "admin_password"
Value: "ваш_новий_пароль"
```

**Session Storage:**
```
Key: "adminPassword"
Value: "поточний_пароль_сесії"
```

### Компоненти

1. **PasswordChangeManager.tsx**
   - Основний компонент для зміни пароля
   - Розташування: `/components/admin/PasswordChangeManager.tsx`
   -Props: `password`, `onPasswordChanged`

2. **AdminPanel.tsx (оновлено)**
   - Додано новий таб "Пароль"
   - Додано попередження про стандартний пароль
   - Додано обробник `handlePasswordChanged`

3. **AdminWelcome.tsx (оновлено)**
   - Оновлено бейдж з посиланням на зміну пароля

### Серверна логіка

**index.tsx:**
```typescript
// Ініціалізація при старті
const DEFAULT_ADMIN_PASSWORD = "admin123";
let ADMIN_PASSWORD = DEFAULT_ADMIN_PASSWORD;

// Завантаження з KV при старті
(async () => {
  const storedPassword = await kv.get("admin_password");
  if (storedPassword) {
    ADMIN_PASSWORD = storedPassword;
  }
})();

// Зміна пароля
app.post("/change-password", async (c) => {
  // Перевірка поточного пароля
  // Валідація нового пароля
  // Збереження в KV
  // Оновлення в пам'яті
});
```

## Рекомендації щодо безпеки

### ✅ Що робити:
- Змініть стандартний пароль відразу після налаштування
- Використовуйте складні паролі (літери, цифри, спецсимволи)
- Регулярно змінюйте пароль (раз на 3-6 місяців)
- Використовуйте унікальний пароль, який ви не використовуєте в інших місцях
- Зберігайте пароль у безпечному менеджері паролів

### ❌ Чого не робити:
- Не використовуйте очевидні паролі (123456, password, admin тощо)
- Не діліться паролем з іншими особами
- Не зберігайте пароль у відкритому вигляді
- Не використовуйте особисту інформацію (дати народження, імена)
- Не використовуйте той самий пароль для інших сервісів

## Приклади безпечних паролів

```
✅ Добре:
- MyC1e@n!ng$ite2025
- Bl!sk-Kl1n1ng#Secure
- 7r0ngP@ssw0rd!2025

❌ Погано:
- admin123
- password
- 123456
- cleaning
- blisk2025
```

## Відновлення доступу

Якщо ви забули пароль:

### Варіант 1: Через KV Store (в Supabase Dashboard)
```
1. Відкрийте Supabase Dashboard
2. Перейдіть до Edge Functions -> KV Store
3. Знайдіть ключ "admin_password"
4. Видаліть або оновіть значення
5. Перезапустіть Edge Function
```

### Варіант 2: Через код (тимчасово)
```typescript
// В /supabase/functions/server/index.tsx
// Тимчасово змініть:
const DEFAULT_ADMIN_PASSWORD = "admin123"; // <- Змініть на новий

// Або видаліть збережений пароль:
await kv.del("admin_password");
```

## Troubleshooting

### Проблема: "Current password is incorrect"
**Рішення:** 
- Переконайтеся, що ви ввели правильний поточний пароль
- Перевірте розкладку клавіатури
- Видаліть зайві пробіли на початку/кінці

### Проблема: "New password must be at least 6 characters"
**Рішення:**
- Збільште довжину пароля до мінімум 6 символів

### Проблема: Пароль змінено, але все ще показується попередження
**Рішення:**
- Оновіть сторінку (F5)
- Перевірте статус в консолі розробника
- Перевірте, чи пароль збережено в KV Store

### Проблема: 401 Unauthorized після зміни пароля
**Рішення:**
- Вийдіть з адмін панелі
- Увійдіть знову з новим паролем
- Очистіть sessionStorage якщо проблема залишається

## FAQ

**Q: Чи можна скинути пароль на стандартний?**
A: Так, просто видаліть ключ "admin_password" з KV Store або встановіть його значення на "admin123".

**Q: Чи зберігається пароль в зашифрованому вигляді?**
A: Пароль зберігається в KV Store. Для production середовища рекомендується додати додаткове шифрування або використовувати Supabase Auth.

**Q: Що станеться якщо забути пароль?**
A: Дивіться розділ "Відновлення доступу" вище.

**Q: Чи можна мати кілька адмін паролів?**
A: В поточній реалізації - ні. Є тільки один пароль адміністратора. Для багатокористувацької системи потрібно інтегрувати Supabase Auth.

**Q: Чи захищений endpoint зміни пароля?**
A: Так, endpoint потребує авторизації з поточним паролем.

## Майбутні покращення

Можливі покращення функціональності:

1. **Історія паролів** - запобігання повторному використанню старих паролів
2. **Двофакторна автентифікація (2FA)** - додатковий рівень безпеки
3. **Політика паролів** - налаштування складності паролів
4. **Логування змін** - журнал змін паролів з датами
5. **Email нотифікації** - повідомлення при зміні пароля
6. **Багатокористувацька система** - різні ролі та паролі
7. **Автоматична деактивація** - logout при зміні пароля на інших пристроях

---

**Дата оновлення:** 10 листопада 2025  
**Версія:** 1.0.0  
**Автор:** Figma Make AI Assistant
