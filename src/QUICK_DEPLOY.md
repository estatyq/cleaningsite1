# ⚡ Швидкий деплой за 5 хвилин

## Крок 1️⃣: GitHub (1 хвилина)

1. Створіть репозиторій: https://github.com/new
   - Назва: `blisk-cleaning`
   - Тип: `Public`
   - Не додавайте README

2. Завантажте проект:
   ```bash
   # Завантажте ZIP з Figma Make та розпакуйте
   # Відкрийте термінал у папці проекту
   
   git init
   git add .
   git commit -m "🎉 БлискКлінінг сайт"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/blisk-cleaning.git
   git push -u origin main
   ```
   
   > Замініть `ВАШ_USERNAME` на ваш логін GitHub

## Крок 2️⃣: Vercel (2 хвилини)

1. Перейдіть: https://vercel.com/signup
2. Увійдіть через GitHub
3. Натисніть "Add New" → "Project"
4. Виберіть `blisk-cleaning` → "Import"
5. Додайте змінні середовища:
   ```
   VITE_SUPABASE_URL=ваш_url
   VITE_SUPABASE_ANON_KEY=ваш_ключ
   ```
   
   > Знайти в Supabase: Settings → API
   
6. "Deploy" → Чекайте 2 хв → Готово! 🎉

## Крок 3️⃣: Supabase Functions (2 хвилини)

```bash
# Встановіть CLI
npm install -g supabase

# Увійдіть
supabase login

# Підключіться
supabase link --project-ref ваш_project_id

# Деплой
supabase functions deploy server
```

## ✅ Перевірка

- [ ] Сайт відкривається
- [ ] Адмін панель `/admin` працює (пароль: `admin123`)
- [ ] Форми працюють
- [ ] Зображення завантажуються

## 🚨 Проблеми?

**Білий екран:**
- Перевірте змінні середовища в Vercel
- Префікс `VITE_` обов'язковий!

**Помилки API:**
- Задеплойте Supabase functions (Крок 3)
- Перевірте в Supabase Dashboard → Edge Functions

**Адмін не працює:**
- Пароль: `admin123`
- Якщо не працює - перевірте консоль (F12)

---

🎉 **Готово! Ваш сайт онлайн!**

Тепер кожен `git push` буде автоматично оновлювати сайт!
